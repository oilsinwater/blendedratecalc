import React, { useState } from 'react';


const Calc = props => {

  const percentage = num => {
    return num / 100;
  }

  const mult = (a, b) => {
    return b * a;
  }

  const $(selector).submit(function (e) { 
    e.preventDefault();
    
  });

}

export default Calc
